<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.se491.eggxact">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Eggxact">
        <activity android:name=".ui.filtering.FilterResults"></activity>
        <activity android:name=".ui.filtering.FiltersActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CrashActivity" />
        <activity android:name=".ui.categoryact.CategoryActivity" />
        <activity android:name=".ui.ratingsact.RatingsActivity" />
        <activity android:name=".AdvSearchActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".LandingPageActivity" />
        <activity android:name=".MainActivity">
        </activity>
        <activity android:name=".RecipeActivity" />
        <activity android:name=".ui.recommendation.RecommendationActivity" />
    </application>
</manifest>